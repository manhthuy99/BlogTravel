import React from 'react'
import Description from '../Description'
import { DataDescription } from '../DescriptionData'

const Descriptions = () => {
    return (
        <div>
            <Description dataDesc={DataDescription}/>
        </div>
    )
}

export default Descriptions
